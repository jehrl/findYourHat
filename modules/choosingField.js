module.exports = {
    chooseWidth : (userInputWidth) => {
    let inputtedWidth = userInputWidth.toString().trim();
    (inputtedWidth, newPlay)
    newPlay.print()
  },
    chooseHeight : (userInputWidth) => {
    let inputtedWidth = inputtedWidth.toString().trim();
    move(input, newPlay)
    newPlay.print()
  },
    chooseHoles : (userInputWidth) => {
    let inputtedWidth = inputtedWidth.toString().trim();
    move(input, newPlay)
    newPlay.print()
  }
}